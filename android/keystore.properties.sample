# keystore.properties 样本文件
# 
# 复制此文件为 keystore.properties 并填入实际的签名信息
# 注意：不要将包含真实密钥信息的 keystore.properties 文件提交到Git仓库！
#
# 用于本地构建签名APK，GitHub Actions会自动从环境变量获取这些信息

# keystore文件路径 (相对于android目录)
storeFile=keystore.jks

# keystore密码
storePassword=your_keystore_password

# 密钥别名
keyAlias=your_key_alias

# 密钥密码
keyPassword=your_key_password
name: bili_you
description: A new Flutter project.

publish_to: 'none'

version: 1.1.515

environment:
  sdk: '>3.0.0-417.0.dev'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # 替换主题包
  flex_color_scheme: ^7.3.1
  # 其他依赖保持不变
  get: ^4.6.5
  flutter_screenutil: ^5.8.4
  # 确保移除 flutter_theme 依赖
  # flutter_theme: ^2.0.0  # 删除这一行

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
